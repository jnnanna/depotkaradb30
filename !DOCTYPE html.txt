<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon E-Commerce</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Custom styles that can't be done with Tailwind */
        .fade-in {
            animation: fadeIn 0.3s ease-in-out;
        }
        
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .card-hover:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        
        .transition-all {
            transition: all 0.3s ease;
        }
    </style>
</head>
<body class="bg-gray-50 min-h-screen">
    <!-- Navigation -->
    <nav class="bg-indigo-600 text-white shadow-lg">
        <div class="container mx-auto px-4 py-3 flex justify-between items-center">
            <a href="#" class="text-2xl font-bold flex items-center">
                <i class="fas fa-store mr-2"></i> E-Shop
            </a>
            <div class="flex items-center space-x-4">
                <div id="auth-buttons">
                    <button onclick="showModal('register-modal')" class="bg-white text-indigo-600 px-4 py-2 rounded-md font-medium hover:bg-indigo-50 transition-all">
                        Inscription
                    </button>
                    <button onclick="showModal('login-modal')" class="ml-2 px-4 py-2 rounded-md font-medium hover:bg-indigo-700 transition-all">
                        Connexion
                    </button>
                </div>
                <div id="user-menu" class="hidden flex items-center">
                    <span id="username-display" class="mr-2 font-medium"></span>
                    <div class="relative">
                        <button id="user-dropdown-btn" class="flex items-center">
                            <div class="w-8 h-8 rounded-full bg-indigo-400 flex items-center justify-center">
                                <i class="fas fa-user text-white"></i>
                            </div>
                            <i class="fas fa-chevron-down ml-1 text-xs"></i>
                        </button>
                        <div id="user-dropdown" class="hidden absolute right-0 mt-2 w-48 bg-white rounded-md shadow-lg py-1 z-50">
                            <a href="#" onclick="showUserArticles()" class="block px-4 py-2 text-gray-800 hover:bg-indigo-50">Mes articles</a>
                            <a href="#" onclick="showAllArticles()" class="block px-4 py-2 text-gray-800 hover:bg-indigo-50">Tous les articles</a>
                            <a href="#" onclick="showAddArticleForm()" class="block px-4 py-2 text-gray-800 hover:bg-indigo-50">Ajouter un article</a>
                            <a href="#" onclick="logout()" class="block px-4 py-2 text-gray-800 hover:bg-indigo-50">Déconnexion</a>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="container mx-auto px-4 py-8">
        <div class="flex justify-between items-center mb-8">
            <h1 id="page-title" class="text-3xl font-bold text-gray-800">Bienvenue sur notre boutique</h1>
            <button id="add-article-btn" onclick="showAddArticleForm()" class="hidden bg-indigo-600 text-white px-4 py-2 rounded-md hover:bg-indigo-700 transition-all">
                <i class="fas fa-plus mr-2"></i> Ajouter un article
            </button>
        </div>
        
        <!-- Articles Grid -->
        <div id="articles-container" class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <!-- Articles will be loaded here dynamically -->
            <div class="text-center py-12 text-gray-500">
                <i class="fas fa-store text-4xl mb-4"></i>
                <p>Connectez-vous pour voir les articles</p>
            </div>
        </div>
    </main>

    <!-- Register Modal -->
    <div id="register-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-lg shadow-xl w-full max-w-md p-6 fade-in">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-2xl font-bold text-gray-800">Créer un compte</h3>
                <button onclick="hideModal('register-modal')" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="register-form" onsubmit="registerUser(event)">
                <div class="mb-4">
                    <label for="register-firstname" class="block text-gray-700 mb-2">Prénom</label>
                    <input type="text" id="register-firstname" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="register-firstname-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-4">
                    <label for="register-lastname" class="block text-gray-700 mb-2">Nom</label>
                    <input type="text" id="register-lastname" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="register-lastname-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-4">
                    <label for="register-email" class="block text-gray-700 mb-2">Email</label>
                    <input type="email" id="register-email" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="register-email-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-4">
                    <label for="register-password" class="block text-gray-700 mb-2">Mot de passe</label>
                    <input type="password" id="register-password" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required minlength="6">
                    <p id="register-password-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-6">
                    <label for="register-confirm-password" class="block text-gray-700 mb-2">Confirmer le mot de passe</label>
                    <input type="password" id="register-confirm-password" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="register-confirm-password-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <button type="submit" class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 transition-all">
                    S'inscrire
                </button>
                <p class="text-center mt-4 text-gray-600">
                    Déjà un compte? <button type="button" onclick="switchToLogin()" class="text-indigo-600 hover:underline">Se connecter</button>
                </p>
            </form>
        </div>
    </div>

    <!-- Login Modal -->
    <div id="login-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-lg shadow-xl w-full max-w-md p-6 fade-in">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-2xl font-bold text-gray-800">Connexion</h3>
                <button onclick="hideModal('login-modal')" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="login-form" onsubmit="loginUser(event)">
                <div class="mb-4">
                    <label for="login-email" class="block text-gray-700 mb-2">Email</label>
                    <input type="email" id="login-email" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="login-email-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-6">
                    <label for="login-password" class="block text-gray-700 mb-2">Mot de passe</label>
                    <input type="password" id="login-password" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="login-password-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <button type="submit" class="w-full bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 transition-all">
                    Se connecter
                </button>
                <p class="text-center mt-4 text-gray-600">
                    Pas encore de compte? <button type="button" onclick="switchToRegister()" class="text-indigo-600 hover:underline">S'inscrire</button>
                </p>
            </form>
        </div>
    </div>

    <!-- Add/Edit Article Modal -->
    <div id="article-form-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-lg shadow-xl w-full max-w-md p-6 fade-in">
            <div class="flex justify-between items-center mb-4">
                <h3 id="article-form-title" class="text-2xl font-bold text-gray-800">Ajouter un article</h3>
                <button onclick="hideModal('article-form-modal')" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <form id="article-form" onsubmit="saveArticle(event)">
                <input type="hidden" id="article-id">
                <div class="mb-4">
                    <label for="article-image" class="block text-gray-700 mb-2">URL de l'image</label>
                    <input type="url" id="article-image" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="article-image-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-4">
                    <label for="article-title" class="block text-gray-700 mb-2">Titre</label>
                    <input type="text" id="article-title" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="article-title-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-4">
                    <label for="article-description" class="block text-gray-700 mb-2">Description</label>
                    <textarea id="article-description" rows="3" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required></textarea>
                    <p id="article-description-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="mb-6">
                    <label for="article-price" class="block text-gray-700 mb-2">Prix (€)</label>
                    <input type="number" id="article-price" step="0.01" min="0" class="w-full px-3 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-indigo-500" required>
                    <p id="article-price-error" class="text-red-500 text-sm mt-1 hidden"></p>
                </div>
                <div class="flex justify-end space-x-3">
                    <button type="button" onclick="hideModal('article-form-modal')" class="px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50 transition-all">
                        Annuler
                    </button>
                    <button type="submit" class="bg-indigo-600 text-white py-2 px-4 rounded-md hover:bg-indigo-700 transition-all">
                        Enregistrer
                    </button>
                </div>
            </form>
        </div>
    </div>

    <!-- Delete Confirmation Modal -->
    <div id="delete-modal" class="fixed inset-0 bg-black bg-opacity-50 flex items-center justify-center z-50 hidden">
        <div class="bg-white rounded-lg shadow-xl w-full max-w-md p-6 fade-in">
            <div class="flex justify-between items-center mb-4">
                <h3 class="text-2xl font-bold text-gray-800">Confirmer la suppression</h3>
                <button onclick="hideModal('delete-modal')" class="text-gray-500 hover:text-gray-700">
                    <i class="fas fa-times"></i>
                </button>
            </div>
            <p class="mb-6 text-gray-700">Êtes-vous sûr de vouloir supprimer cet article? Cette action est irréversible.</p>
            <div class="flex justify-end space-x-3">
                <button onclick="hideModal('delete-modal')" class="px-4 py-2 border border-gray-300 rounded-md text-gray-700 hover:bg-gray-50 transition-all">
                    Annuler
                </button>
                <button id="confirm-delete" class="bg-red-600 text-white py-2 px-4 rounded-md hover:bg-red-700 transition-all">
                    Supprimer
                </button>
            </div>
        </div>
    </div>

    <script>
        // Initialize data in localStorage if not exists
        if (!localStorage.getItem('users')) {
            localStorage.setItem('users', JSON.stringify([]));
        }
        
        if (!localStorage.getItem('articles')) {
            localStorage.setItem('articles', JSON.stringify([]));
        }
        
        // DOM Elements
        const authButtons = document.getElementById('auth-buttons');
        const userMenu = document.getElementById('user-menu');
        const usernameDisplay = document.getElementById('username-display');
        const userDropdownBtn = document.getElementById('user-dropdown-btn');
        const userDropdown = document.getElementById('user-dropdown');
        const
</html>